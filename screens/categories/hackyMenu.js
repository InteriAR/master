import React from 'react'
import { StyleSheet, Text, View, FlatList, TouchableOpacity, Alert, Image } from 'react-native'

function HackyMenu(props) {
  return (
    null
  )
}

export default HackyMenu

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  item: {
    backgroundColor: '#f9c2ff',
    padding: 20,
    marginVertical: 8,
    marginHorizontal: 16,
  },
  title: {
    fontSize: 32,
  },
  loading: {
    fontSize: 40,
    color: 'blue',

  }
});

